<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>UX at UCLA</title>
    <link href="https://fonts.googleapis.com/css2?family=Migra&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Migra', sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f9f9f9;
            text-align: center;
        }
        header {
            background-color: #4a90e2;
            color: white;
            padding: 20px 0;
        }
        h1 {
            margin: 0;
            font-size: 2.5em;
        }
        .upload-section {
            margin: 40px auto;
            padding: 20px;
            max-width: 600px;
            background: white;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }
        .upload-section input[type="file"] {
            display: none;
        }
        .upload-label {
            display: inline-block;
            padding: 10px 20px;
            color: white;
            background-color: #4a90e2;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1em;
            margin-bottom: 10px;
        }
        .upload-label:hover {
            background-color: #357abd;
        }
    </style>
</head>
<body>
    <header>
        <h1>UX at UCLA</h1>
    </header>
    <section class="upload-section">
        <h2>Upload Your Files</h2>
        <form action="upload.php" method="post" enctype="multipart/form-data">
            <label class="upload-label" for="file-upload">Choose PDF or JPEG</label>
            <input type="file" id="file-upload" name="uploadedFile" accept="application/pdf, image/jpeg">
            <br>
            <button type="submit">Upload</button>
        </form>
    </section>
    <script>
        // Verify font loading in browser console
        if (document.fonts) {
            document.fonts.load('1em Migra').then(() => {
                console.log('Migra font loaded successfully.');
            }).catch(() => {
                console.error('Migra font failed to load.');
            });
        }
    </script>
</body>
</html>
